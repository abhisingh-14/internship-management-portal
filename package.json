{
  "name": "internship-management-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready Internship Management Portal (React SPA + Express API + MySQL)",
  "scripts": {
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "install-all": "npm run install:server && npm run install:client",
    "build:client": "cd client && npm run build",
    "build": "npm run install-all && npm run build:client",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm run dev",
    "create-admin": "cd server && npm run create-admin",
    "dev:client": "cd client && npm run dev",
    "lint:server": "cd server && npm run lint",
    "lint:client": "cd client && npm run lint"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
